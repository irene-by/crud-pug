extends layout

block content
    h1 Все курсы Академии

    form(method="post")
        fieldset.form-group
            label(for="name") Name:
            input.form-control(name="name", type="text", id="name" placeholder="введите имя курса")
            small.text-muted Дообавьте свой новый кус
        button.btn.btn-outline-primary(type="submit") Создаь курс
        a.btn.btn-defoult(href='/courses') Отмена